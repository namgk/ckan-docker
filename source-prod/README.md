# CKAN Production from source
Running Apache2 with wsgi interface to CKAN.

To run, link this container with running postgres and solr containers, their host names should be ckan-postgres and ckan-solr
